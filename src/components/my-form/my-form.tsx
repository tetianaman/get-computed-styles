import classNames from 'classnames';
import styles from './my-form.module.scss';
import { Button } from '../button/button';

export const MyForm = () => {
    return <form id={styles.userForm} className={styles.myForm}>
        <Button /></form>;
};
